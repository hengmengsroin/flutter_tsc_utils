# Example App

Run the example app with:

```bash
cd example
flutter pub get
flutter run
```

The example shows:

- A live label preview
- TSPL command generation
- Khmer text rendered through `khmerText()`

For the best Khmer rendering, install or bundle a Khmer-capable font such as `Noto Sans Khmer`.
